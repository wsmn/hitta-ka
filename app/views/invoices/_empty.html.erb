<div class="p-6 text-center">
  <div class="w-40 mx-auto mt-4 mb-4">
    <%= image_tag('no_invoices.svg', alt: 'Builders')%>
  </div>
  <p class="mb-4 text-2xl font-medium text-gray-900">You havn't created any invoices yet!</p>
  <p class="max-w-xs mx-auto mb-6 text-gray">Create your first invoice by pressing the button down below.</p>
  <div class="flex justify-center mt-8 text-center text-gray">
    <div class="flex font-medium hover:border-gray-400 hover:text-gray-400">
      <%= link_to(projects_path, class: 'bg-blue-600 border-blue-600 border hover:bg-blue-500 hover:border-blue-500 py-2 px-3 rounded text-white') do %>
        <%= t('.create_invoice') %>
      <% end %>
    </div>
  </div>
</div>
