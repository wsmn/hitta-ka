<% content_for(:sidebar) do %>
  <%= render('application/account_menu', current: :invoices) %>
<% end %>
<% content_for(:header_title) do %>
  <%= t('.title') %>
<% end %>
<div class="w-full p-2 overflow-y-auto lg:flex-grow lg:p-4 xl:p-6">
  <div class="flex flex-wrap">
    <div class="flex-grow mx-auto">
      <div class="flex flex-wrap w-full bg-white border border-gray-500 rounded-md">
        <%= render('form', customers: @customers, customer: @customer, invoice: @invoice) %>
      </div>
    </div>
  </div>
</div>
