<div class="w-full flex justify-between bg-white border-b-2 border-blue-500 p-4 lg:hidden">
  <div class="mb-1">
    <h1 class="text-3xl font-bold text-gray-800"><%= t('.title') %></h1>
    <h2 class="text-xl font-medium text-blue-600 -mt-1">All projects</h2>
  </div>
  <div class="flex items-start">
    <%= link_to(new_project_path, class: 'btn btn-blue normal-case text-base') do %>
      <%= t('.new_project') %>
    <% end %>
  </div>
</div>
