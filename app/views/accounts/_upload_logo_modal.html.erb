<div x-show="upload_modal" class="overflow-y-auto bg-gray-900" style="background-color: rgba(0,0,0,0.5)"
    :class="{ 'absolute inset-0 z-50 flex items-center justify-center': upload_modal }">
    <div x-show="upload_modal" @click.away="upload_modal = false"
        class="bg-white rounded-md xl:w-1/3 opacity-100 flex items-center justify-center p-8 relative">
        <div
            class="border-dashed border-2 border-gray-500 rounded-md w-full flex-col flex items-center justify-center text-center p-6">
            <div class="">
                <svg class="h-16 w-16 text-gray-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5"
                        d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                </svg>
            </div>
            <div class="mt-2 text-gray-700">
                <span class="text-blue-500 font-medium">Upload your logo</span> or drag and <p>drop it in the box.</p><p class="font-light text-sm mt-1">PNG, JPG up to 2 MB</p>
            </div>
        </div>
        <div
            class="absolute top-0 right-0 bg-red-600 w-8 h-8 rounded-full text-white font-bold flex items-center justify-center -mt-4 -mr-4 cursor-pointer"
            @click="upload_modal = false">
            <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M6 18L18 6M6 6l12 12" />
            </svg>
        </div>
    </div>
</div>
